"use strict";
//Ternary operator
// ? :
//exp ? res1 : res 2
let val1 = 100, val2 = 200;
let result = (val1 > val2) ? 100 : 200;
console.log(result);
//Ex 2
let personAge = -1;
let ans = (personAge > 0 && personAge >= 18) ? 'Adult' : "minor";
console.log(ans);
